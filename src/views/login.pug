extends base


block content 
    if errorMessage 
        span=errorMessage 
    form(method="POST")
        input(palceholder="Username", name="username", type="text", required)
        input(palceholder="Password", name="password", type="password", required)
        input(type="submit", value="Login")
    hr
    div 
        span Don't have an account?
        a(href="/join") Create one now $rarr;